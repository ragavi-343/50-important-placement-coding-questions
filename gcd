
#include <stdio.h>
int main() {
    int a,b;
    scanf("%d %d",&a,&b);
    int r=a<b?a:b;
    if(r<=0)
    {
        printf("%d",0);
    }
    else
    {
   while(r>0)
   {
       if(a%r==0 && b%r==0)
       {
           printf("%d",r);
           break;
       }
       r--;
   }
  }
    return 0;
}
