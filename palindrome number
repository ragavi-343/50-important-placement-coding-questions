#include <stdio.h>
int main() {
    int a;
    scanf("%d",&a);
    int n=a;
    int s=0;
    while(n>0)
    {
        int r=n%10;
        s=s*10+r;
        n/=10;
    }
    if(s==a)
    {
    printf("palindrome number");
    }
    else
    {
        printf("not a palindrome");
    }

    return 0;
}
