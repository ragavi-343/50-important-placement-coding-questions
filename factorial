#include <stdio.h>
int fact(int num)
{
    if(num==0 || num==1)
    return 1;
    else
    {
        return num*fact(num-1);
    }
}
int main() {
    int n;
    scanf("%d",&n);
    int r=fact(n);
    printf("%d",r);
    return 0;
}
