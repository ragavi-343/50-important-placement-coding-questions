#include <stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int ans=0,i=1;
    while(n!=0)
    {
        ans=ans+(n%10)*i;
        n/=10;
        i*=2;
    }
    printf("%d",ans);
    return 0;
}
